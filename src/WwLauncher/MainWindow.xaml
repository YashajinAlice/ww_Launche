<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WwLauncher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WwLauncher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WW Launcher">

    <Grid Padding="32" RowDefinitions="Auto,Auto,*,Auto" RowSpacing="16">
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="WW Launcher" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="VersionText" Opacity="0.7" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button x:Name="CheckUpdateButton" Content="檢查更新" Click="CheckUpdateButton_Click" />
            <Button x:Name="LaunchButton" Content="啟動（尚未接線）" IsEnabled="False" />
        </StackPanel>

        <Border Grid.Row="2" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
            <ScrollViewer>
                <TextBlock x:Name="StatusText" TextWrapping="WrapWholeWords" IsTextSelectionEnabled="True" />
            </ScrollViewer>
        </Border>

        <ProgressBar
            x:Name="UpdateProgress"
            Grid.Row="3"
            Minimum="0"
            Maximum="100"
            Visibility="Collapsed"
            IsIndeterminate="False" />
    </Grid>
</Window>
