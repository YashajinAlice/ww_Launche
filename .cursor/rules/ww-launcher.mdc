---
description: WW Launcher 專案定位與技術棧（WinUI 3 + C#）
alwaysApply: true
---

# WW Launcher（ww_Launche）

- 本倉庫是 **WW 啟動器**，技術棧固定為 **WinUI 3 + C#（Windows App SDK）**。
- 目前為 **unpackaged**（`WindowsPackageType=None`），目標框架 `net8.0-windows10.0.19041.0`。
- 預設平台 **x64**（亦支援 ARM64）；建置請用 `-p:Platform=x64`。
- 版本更新：`Services/UpdateService` + `update-manifest.sample.json`；遠端用環境變數 `WW_LAUNCHER_UPDATE_URL`。
- 下載安裝、遊戲啟動流程尚未接線；擴充時延續現有 Models/Services 分層，避免把邏輯塞進 XAML code-behind。
- 回覆使用者時使用**繁體中文**。
